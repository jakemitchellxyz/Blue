@extends('Blue::auth.layouts.html')

@section('body')
div.container
    div.login-box
        div
            div.login-form.row
                div.col-sm-12.text-center.login-header
                    i.login-logo.fa.fa-first-order.fa-5x
                    h3.login-title Let's Get Started.
                div.col-sm-12
                    div.card.login-body
                        div.card-body(style="padding:0 10px")
                            div#login-progress.progress.hidden
                                div.progress-bar.progress-bar-success.progress-bar-striped.active(role="progressbar" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100" style="width: 100%")
                                    | Working some magic...
                            {{ Form::open(['url' => url('new'), 'id' => 'form-basic-info']) }}
                            div.form-group
                                div.sub-title Site Name
                                input.form-control(name="site-title" type="text" placeholder="My Awesome Site" value="{{ old('site-title') }}")
                            div.form-group
                                div.sub-title Username
                                input.form-control(name="username" type="text" placeholder="BillyBob23")
                            div.form-group
                                div.sub-title Password
                                input.form-control(name="password" type="password" placeholder="{{ '•••••••' }}")
                            div.login-button.text-center
                                button.btn.btn-primary(type="submit") Next&nbsp;
                                    i.fa.fa-angle-double-right
                            {{ Form::close() }}
@endsection